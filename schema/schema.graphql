schema {
    query: Query
    mutation: Mutation
}

type Query {
    hello: String!
}

type Mutation {
    createUser(userInfo: CreateUserInfo!): RegisterUser
    login(username: String!, password: String!): LoginRes
}