schema {
    query: Query
    mutation: Mutation
}

type Query {
    hello: String!
}

type Mutation {
    createUser(userInfo: CreateUserInfo!): User
    login(username: String!, password: String!): LoginRes
}